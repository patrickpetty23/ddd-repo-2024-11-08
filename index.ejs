<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dr. Deagan Donut Company</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f5f5f5;
      color: #333;
      margin: 0;
      padding: 0;
    }

    h1 {
      text-align: center;
      padding: 20px;
      background-color: #ff6f61;
      color: white;
      margin: 0;
    }

    p {
      text-align: center;
      font-size: 1.2rem;
      margin: 10px;
      color: #666;
    }

    .container-list {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      padding: 20px;
    }

    .donut-item {
      background-color: white;
      margin: 20px;
      padding: 15px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    /* Responsive design for mobile devices */
    @media (max-width: 768px) {
      .donut-item {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <h1>Dr. Deagan Donut Company</h1>
  <p>Discover our delicious variety of donuts</p>
  <div class="container-list" id="donutList">
  <% for (let iCount = 0; iCount < donuts.length; iCount++) { %>
    <div class="donut-item">
      <img src="<%= donuts[iCount].url %>" alt="<%= donuts[iCount].donut_name %>" class="donut-image">
      <h3><%= donuts[iCount].donut_name %></h3>
      <p>Delicious and freshly made!</p>
    </div>
  <% } %>
</div>
</body>
</html>
